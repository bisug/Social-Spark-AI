{
  "name": "social-spark-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && npx esbuild index.tsx --bundle --outfile=dist/index.js --loader:.tsx=tsx --define:process.env.API_KEY=\\\"$API_KEY\\\" --external:react --external:react-dom/* --external:react/* --external:@google/genai"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}